# Handoff — Fer Bittencourt portfolio ("Tenebrae")

**Date:** 2026-08-25 · **Repo:** `~/Documents/fernando-portfolio`
**Language:** user writes EN and PT-BR; reply in whichever he used last (last was EN).

## Where the work stands

Yesterday's handoff said "nothing is committed." That is fixed. The repo is now a git repo with
17 commits on `main`, a glossary, and eleven ADRs. **The paper trail is the thing to read first —
this document deliberately does not repeat it.**

The session ran `/grill-with-docs` to settle the direction, then took the step-2 detour into
prototyping to answer questions that needed running code. That detour did its job: the two decks,
the Vigil, the altar and the room all came out of it. **The detour has run long. The next step is
`/to-spec`, not more prototype polish.**

Fernando is designing the faceplate artwork himself in parallel.

## Read these first — do not re-derive

| What | Where |
|---|---|
| Glossary — the project's language | `CONTEXT.md` |
| Eleven decisions, including four reversals | `docs/adr/0001..0011` |
| Product truth — still valid, do not rewrite | `PRODUCT.md` |
| Prototype workbench, dials, known divergences | `prototype/README.md` |
| Plate art provenance and rejected candidates | `prototype/ornament/SOURCES.md` |
| Dead July direction, kept as anti-reference | `docs/archive/` |
| Why each thing was done | the commit messages — they are written to be read |

`git log --oneline` is a readable narrative of the day. Use it.

## The four reversals a fresh agent would otherwise "fix" back

1. **three.js is deliberately back** after being stripped in July (ADR-0003).
2. **The Crossfader's index mark must not track the fader** (ADR-0005).
3. **The Bend corruption mechanic is dead.** Do not reintroduce `?bend=` or the melt shader (ADR-0006).
4. **Orbit was banned, then un-banned** (ADR-0007 as amended, then ADR-0011). The *camera* moves;
   the *Unit* never does. There is no INSPECT.

## Running it

```
npm run prototype     # http://localhost:5174/?title=unifraktur
```

Drag empty space to move the camera; drag the Unit to operate it. HUD dials tune the Plate
engraving, the relief, the plate art framing, the camera, and the Vigil.

`window.__unit` is a workbench hook — `pads()`, `parts()`, `screenOf(mesh)`, `render()`, `cam`,
`setCam()`, `setEng()`, `setTitle()`. It exists because rAF is throttled in a background tab, so the
last painted frame's matrices cannot be trusted. All four control paths were verified in a real
browser on 2026-08-25; they had never been clicked before.

## Blocked on Fernando

- **Faceplate artwork.** He is designing it. `prototype/ornament/README.md` says what to drop in.
  Currently standing in: De Wit's *Planisphaerium coeleste* (1650, public domain).
- **`RACK` contents.** Slot 4 must list tools he actually uses, and `PRODUCT.md` forbids inventing
  them. He was asked and redirected to layout work instead. Still open.
- **GitHub repo.** He chose "public on GitHub" but `gh repo create` was never run. Needs his go-ahead
  before anything is pushed.

## Open work, roughly in order

1. **`/to-spec` → `/to-tickets`.** Overdue. The room, the five polish parts, the content, and the
   `src/` build map onto tickets with real blocking edges.
2. **`src/` is still a placeholder shell.** The DOM truth layer (ADR-0002) does not exist yet: six
   Modules as semantic HTML, Pads as real buttons, one content source feeding both DOM and Screen.
   Completely disjoint from the prototype — safe to run as a parallel agent.
3. **Module content.** Placeholder copy written into the prototype. ADR-0009 killed browsing, so
   **every Module must fit one Screen exactly.**
4. **Deck faces** should become real volvelle rings. The *Calendarium Perpetuum* on his `engravings`
   are.na board is genuine 17th-century public domain and is the right source; not yet fetched at
   high resolution. Current faces are hand-drawn.

## Known rough edges in the prototype

- The lancet window **reads rectangular** — the arch sits above frame at most camera angles.
- **Only the right curtain shows**; the left one's rotation is wrong.
- **No shadows anywhere.** Biggest single realism win available; things sit flat on the desk.
- No painted vault, pictures or chandelier — the reference study has all three.
- Warm candlelight **tints the plate art brown**. Watch this when his artwork lands.
- Geometry is hand-written primitives. It holds to ~74° off vertical, not beyond; the camera is
  clamped for exactly that reason.
- Contact email is hard-coded in `scene.js`. It is his public contact, but treat it as PII.

## One structural thing nobody has decided

**The Vigil is doing two jobs**: candles going out one by one (the rite, interior) and day turning to
night (the sky, exterior). They currently move together on one control. It reads fine now, but
splitting them later — dusk with candles lit, or a dark room in daylight — gets expensive if it goes
unnoticed.

## Cautions

- **Descriptions do not land with this user. Build and show.** Confirmed repeatedly today. Every
  direction that stuck was settled by a render, not a paragraph.
- **He supplies references and reacts.** Do not ask him to choose between described options when you
  could show him two renders instead.
- **Check provenance before praising a reference.** Two images on his `engravings` board are
  unusable — one carries a Dreamstime watermark, one is a signed contemporary illustration. Both were
  recommended before being looked at properly. See `prototype/ornament/SOURCES.md`.
- **Verify at render size.** The Plate texture is 2048px wide but draws ~590px on screen. Fine line
  work below ~5px of texture weight disappears entirely, and dark-on-dark is invisible.
- Do not re-litigate settled ground. He has moved on, repeatedly and fast.
- He said explicitly: *"you don't need to sweat too much in being perfect right now, let's just
  create the sketch of everything step by step."* Match that pace.

## Suggested skills

1. **`/to-spec`** — first. Collapse this thread and the ADRs into a buildable plan.
2. **`/to-tickets`** — then split it. The polish parts and the `src/` build have real blocking edges.
3. **`/implement`** per ticket, fresh context each time.
4. **`/prototype`** — only if a specific design question needs running code again. The prototype
   already exists as a workbench; extend it rather than starting over.
5. **`/domain-modeling`** — pulled in underneath any work that changes the Unit's anatomy. It changed
   twice today and `CONTEXT.md` was updated both times. Keep that habit.

Not applicable: `/triage` (no incoming issues), `/diagnosing-bugs` (nothing broken),
`/improve-codebase-architecture` (`src/` is empty), `/wayfinder` (the fog cleared this morning).
